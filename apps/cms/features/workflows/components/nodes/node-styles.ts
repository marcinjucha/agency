/**
 * Shared node styling constants.
 * borderColors and nodeIcons are derived from node-registry.ts (single source of truth).
 */

export const nodeBaseClasses =
  'bg-card border border-border rounded-lg px-4 py-3 min-w-[180px]'

export const selectedClasses = 'ring-2 ring-primary/50'

export { borderColors, nodeIcons } from './node-registry'
