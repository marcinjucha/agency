// LandingPage reduced to { id, slug, cta_url } — canonical definition in @agency/database.
// The landing page content is hardcoded in the website; only the survey CTA URL is editable here.
export type { LandingPage } from '@agency/database'
export { toLandingPage } from '@agency/database'
